本示例使用到的资源：
LED          ------ PB1
I2C1_SCL     ------ PB3
I2C1_SDA     ------ PB4


本示例的功能流程：
1、上电配置系统时钟（RCH作为时钟源）为48M；
2、初始化LED的GPIO，并初始状态为常灭，初始化I2C1的GPIO；
3、初始化I2C1，使能I2C1；
4、控制LED常亮；
5、等待地址匹配，接收8个字节数据，检测停止位后清除停止位标志；
6、等待地址匹配，发送8个字节数据，检测停止位后清除停止位标志和NACKF标志。

注意：
当LED点亮时，再控制主机发起通信。

