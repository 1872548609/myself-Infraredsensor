 本示例使用到的资源：
LED        --   PB1

本示例功能流程：
1、上电配置系统时钟(RCH、48M)，初始化LED；
2、初始化IWDG：开启RCL时钟，使能写权限，配置溢出时间为2.048s，喂狗使配置生效后使能IWDG；
3、每1500ms进行一次IWDG重载计数，LED以相同频率闪烁，如果在IWDG计数器计数到0之前没有重载计数，IWDG复位。

注意：
1、IWDG喂狗不及时会导致复位，因此建议全速运行观测效果。