本示例使用到的资源：
无


本示例的功能流程：
1、上电配置系统时钟（RCH作为时钟源）48M；
2、初始化LPTIM1：工作时钟选择RCL；
3、使能LPTIM1的NVIC中断；
4、使能LPTIM1的自动重载匹配中断，填充自动重载值，启动单次计数；
   LPTIM1使用工作时钟为RCL，因此计数1s的自动重载值为(0x7D00 - 1) = 0x7CFF
5、LPTIM1 1s后产生一次自动重载匹配中断，查询到ARRM置1后清除标志位。

