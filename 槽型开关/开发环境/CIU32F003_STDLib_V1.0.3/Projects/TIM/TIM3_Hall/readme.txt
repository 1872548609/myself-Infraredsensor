本示例使用到的资源：
TIM3 CH1     --   PA5
TIM3 CH2     --   PA4
TIM3 CH3     --   PA3


本示例的功能流程：
1、上电配置时钟（RCH 48MHz）；
2、配置TIM3 通道1输入源为TRC，且双沿有效；
3、配置从模式为复位模式，且触发源为TI1F_ED；
4、当TI1、TI2、TI3通道信号有沿翻转时，将触发TIM3 CNT复位，且捕获复位前的CNT值到CC1寄存器中；


注意：
1、本示例仅展示一组触发信号捕获值（g_ch1_cc1_value[6]）；
2、第一次捕获值的CC1值可忽略。